export const rootRoute = '/';

export const publicRoutes = [
  '/services',
  '/freelance-jobs',
  '/support',
  '/new-verification',
];

export const authRoutes = ['/login', '/signup', '/error', '/forgot-password'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT = '/dashboard';
